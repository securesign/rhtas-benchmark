apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: k6-pushgateway-monitor
  namespace: "{{ baseline_namespace }}"
spec:
  selector:
    matchLabels:
      app: pushgateway
  endpoints:
  - port: http
    interval: 5s
    path: /metrics