apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  namespace: "{{ baseline_namespace }}"
  labels:
    app: pushgateway
spec:
  selector:
    app: pushgateway
  ports:
  - name: http
    port: 9091
    targetPort: http-metrics