apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ monitoring.grafana.service_account }}
  namespace: {{ baseline_namespace }}
  labels:
    app: tas-monitoring
    component: grafana
  annotations:
    description: "ServiceAccount for TAS Grafana to access Prometheus metrics"
    managed-by: "tas-baseline-infrastructure"
automountServiceAccountToken: true